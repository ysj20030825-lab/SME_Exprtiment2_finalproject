# 스마트모빌리티공학실험2 Final Project Report

## 1. 모티베이션 & 인트로

본 프로젝트의 목표는 18개 Wi-Fi RTT 앵커가 측정한 거리값과 앵커 좌표를 이용하여 사용자의 2차원 위치를 추정하는 것이다. 제공 데이터에는 전체 사용자 1000명 중 700명의 정답 위치가 포함되어 있으며, 나머지 300명은 hidden test set으로 사용된다. 따라서 최종 알고리즘은 제공된 700개 데이터에만 과도하게 맞춰지는 것이 아니라, 보이지 않는 데이터에서도 안정적으로 동작해야 한다.

초기에는 가장 기본적인 거리 기반 위치 추정 방법인 삼변측량, Linear Least Squares, Weighted Least Squares 계열 방법을 검토하였다. 이러한 방식은 각 앵커의 RTT 측정값을 실제 거리로 보고, 여러 거리 방정식을 가장 잘 만족하는 위치를 찾는다. 그러나 데이터 분석 결과, RTT 측정값은 실제 거리보다 크게 측정되는 양의 바이어스가 강하게 나타났다. 또한 일부 앵커에서는 큰 이상치가 포함되어 있었고, 앵커별 오차 분포도 서로 다르게 나타났다. 이 때문에 모든 앵커의 거리값을 하나의 정확한 반지름처럼 사용하는 방식은 최종 위치 추정에 한계가 있다고 판단하였다.

중간발표 단계에서는 Random Forest를 이용하여 RTT 측정값의 비선형적인 오차 구조를 학습하는 방향을 실험하였다. Random Forest는 단순 선형 모델보다 복잡한 오차 패턴을 반영할 수 있다는 장점이 있었다. 실제 내부 검증에서도 Random Forest 직접 회귀는 MAE 7.25 m, RMSE 8.76 m, Median error 6.27 m, P95 error 17.87 m로 나타나 단순 거리 방정식 기반 방식보다 일부 지표에서 개선을 보였다. 이는 RTT 측정값과 위치 사이에 단순 선형식으로 설명하기 어려운 비선형 관계가 존재한다는 점을 보여준다.

그러나 Random Forest가 x, y 좌표를 직접 예측하는 방식은 최종 알고리즘으로 사용하기에는 한계가 있다고 판단하였다. 첫째, 모델이 위치를 바로 회귀하기 때문에 예측된 좌표가 각 앵커의 거리 측정값과 물리적으로 얼마나 일관적인지 설명하기 어렵다. 둘째, 제공된 700개 데이터의 위치 분포를 학습하는 성격이 강해 hidden test set에서 안정성이 떨어질 수 있다. 셋째, 본 데이터의 핵심 문제는 단순히 d_hat에서 x, y를 맞히는 것이 아니라, RTT 측정값이 실제 거리보다 크게 측정되는 불확실성을 어떻게 위치 추정 구조 안에 반영할 것인가라고 판단하였다.

이 지점에서 단순히 Random Forest의 입력 feature를 늘리거나, RF 결과에 후처리 보정을 붙이는 방향도 가능했다. 하지만 이 경우 최종 구조가 여전히 “RTT 입력에서 좌표를 직접 회귀한 뒤 보정하는 방식”에 머무르게 된다. 또한 LS, WLS, RANSAC, robust LS와 같은 거리 방정식 기반 접근은 위치 추정 문제에서 자연스럽게 떠올릴 수 있는 대표적인 방법이므로, 본 프로젝트의 창의성 측면에서는 차별성이 충분하지 않다고 판단하였다. 따라서 본인은 성능 개선뿐 아니라 알고리즘의 해석 방식 자체를 바꾸는 방향이 필요하다고 보았다.

중간 단계에서 동시에 검토하던 아이디어 중 하나가 확률맵 기반 위치 추정이었다. 확률맵 기반 접근은 하나의 좌표를 바로 예측하는 대신, 2차원 공간 전체에 대해 “각 위치 후보가 얼마나 그럴듯한가”를 점수화하는 방식이다. 이 관점은 본 데이터셋의 특성과 잘 맞는다고 판단하였다. RTT 측정값에 양의 바이어스와 이상치가 포함되어 있다면, 측정값을 하나의 정확한 거리 원으로 강제하기보다는, 실제 위치가 존재할 가능성이 높은 공간 영역을 만들고 여러 앵커의 정보를 누적하는 방식이 더 적합하기 때문이다.

따라서 본 프로젝트에서는 Random Forest 실험을 통해 확인한 “RTT 오차가 비선형적이고 앵커별로 다르다”는 사실을 확률맵 기반 위치 추정 구조에 반영하였다. 구체적으로는 각 앵커의 RTT 측정값을 하나의 원 반지름으로 보지 않고, 학습 데이터에서 관찰된 앵커별 오차 분포를 이용하여 “실제 거리가 존재할 가능성이 높은 거리 구간”으로 바꾸는 방식을 설계하였다. 이 거리 구간은 2차원 평면에서는 하나의 원이 아니라 두께를 가진 고리형 영역이 된다. 이후 여러 앵커의 고리형 영역을 공간적으로 누적하면, 각 위치 후보가 얼마나 그럴듯한지를 나타내는 신뢰도장을 만들 수 있다.

최종 알고리즘은 CF-AQARF, 즉 Coarse-to-Fine Adaptive Quantile Annulus Reliability Field로 정의하였다. 해석하자면 Coarse-to-Fine 적응형 분위수 고리형 신뢰도장 위치 추정 알고리즘이다. 이 알고리즘은 RTT 측정값을 단일 거리 원으로 보지 않고, 학습 데이터에서 계산한 앵커별 RTT 오차 분위수를 이용하여 두께를 가진 고리형 신뢰 영역으로 변환한다. 또한 입력 RTT의 안정성에 따라 narrow, normal, wide band 중 하나를 선택하고, 전체 후보 공간을 coarse-to-fine 방식으로 탐색하여 계산량과 정밀도 사이의 균형을 맞춘다.

최종적으로 CF-AQARF를 선택한 이유는 세 가지이다. 첫째, Random Forest처럼 좌표를 직접 회귀하지 않으면서도 학습 데이터에서 얻은 오차 분포를 활용할 수 있다. 둘째, LS나 WLS처럼 거리 잔차를 최소화하는 방식보다 RTT의 양의 바이어스와 불확실성을 직접 표현할 수 있다. 셋째, 단순 거리 기반 방식이 아니라 분위수 기반 고리형 신뢰 영역과 공간 신뢰도장을 결합하므로, 본 프로젝트의 창의성 평가 측면에서도 차별성이 있다고 판단하였다.

| 구분                  | 기존 접근                   | 한계                     | 본 프로젝트의 접근                             |
| ------------------- | ----------------------- | ---------------------- | -------------------------------------- |
| Linear LS           | RTT 측정값을 실제 거리 반지름으로 사용 | 양의 바이어스와 이상치에 취약       | RTT 오차 분위수로 거리 범위 생성                   |
| WLS / IRWLS         | 앵커별 가중치로 거리 잔차 최소화      | 최종적으로는 거리 잔차 최소화에 의존   | 잔차 최소화 대신 공간 신뢰도장 생성                   |
| Random Forest 직접 회귀 | RTT에서 x, y 좌표 직접 예측     | 물리적 거리 제약 반영이 약함       | 직접 회귀 대신 앵커별 신뢰 영역 누적                  |
| CF-AQARF            | 고리형 신뢰 영역 기반 공간 점수화     | 격자 해상도와 일부 극단 샘플 영향 존재 | adaptive band와 coarse-to-fine 탐색으로 안정화 |


## 2. 알고리즘 설명

### 2.1 실제 거리와 RTT 오차 정의

i번째 앵커의 위치를 b_i = [x_i, y_i]^T, u번째 사용자의 정답 위치를 p_u = [x_u, y_u]^T라고 하면 실제 거리는 다음과 같이 계산된다.

r_i,u = ||p_u - b_i||_2 = sqrt((x_u - x_i)^2 + (y_u - y_i)^2)

RTT 측정 거리 d_hat_i,u와 실제 거리의 차이를 RTT 오차로 정의한다.

e_i,u = d_hat_i,u - r_i,u

여기서 e_i,u > 0이면 측정 거리가 실제 거리보다 크게 측정되었다는 의미이고, e_i,u < 0이면 측정 거리가 실제 거리보다 작게 측정되었다는 의미이다. 제공 데이터 분석 결과, 양의 오차가 많이 나타났으므로 RTT 측정값을 실제 거리 자체로 보기보다는 실제 거리보다 길게 측정된 값으로 해석하는 것이 더 적합하다고 판단하였다.

### 2.2 분위수 기반 오차 구간

분위수는 데이터를 작은 값부터 큰 값까지 정렬했을 때 특정 비율에 해당하는 값이다. 예를 들어 0.25 분위수는 전체 데이터 중 약 25%가 그 값보다 작거나 같은 지점이고, 0.90 분위수는 전체 데이터 중 약 90%가 그 값보다 작거나 같은 지점이다. 평균은 큰 이상치에 민감하지만, 분위수는 데이터의 범위와 퍼짐을 표현하는 데 유리하다.

본 알고리즘은 각 앵커별로 RTT 오차 분포를 계산하고, 이 분포에서 하위 분위수와 상위 분위수를 구한다. i번째 앵커에 대해 모든 학습 사용자에 대한 오차 집합을 다음과 같이 둔다.

E_i = {e_i,1, e_i,2, ..., e_i,N}

여기서 q_low와 q_high는 분위수를 계산할 때 사용하는 비율값이다. 예를 들어 q_low = 0.25, q_high = 0.90이면, 해당 앵커의 오차 분포에서 하위 25% 지점과 하위 90% 지점을 찾는다는 의미이다.

Q_i^low = quantile(E_i, q_low)

Q_i^high = quantile(E_i, q_high)

이때 Q_i^low와 Q_i^high는 모든 앵커에 대해 같은 값이 아니다. 같은 q_low와 q_high를 사용하더라도, 실제로 계산되는 Q_i^low와 Q_i^high는 앵커 i마다 다르다. 즉, 분위수 비율 q_low, q_high는 전체 알고리즘에서 정한 기준이고, Q_i^low, Q_i^high는 각 앵커의 실제 오차 분포에서 계산된 결과값이다.

예를 들어 Normal band에서 q_low = 0.25, q_high = 0.90을 사용한다고 하자. 1번 앵커의 오차가 전체적으로 작고 안정적이면 Q_1^low와 Q_1^high의 차이는 작을 수 있다. 반대로 8번 앵커의 오차가 크고 불안정하면 Q_8^low와 Q_8^high의 차이는 커질 수 있다. 따라서 같은 Normal band를 사용하더라도 실제 고리 폭은 앵커마다 다르게 정해진다.

### 2.3 band가 생성되는 원리

본 알고리즘에서는 입력 RTT의 안정성에 따라 세 가지 분위수 band를 사용하였다. band는 학습 데이터의 오차 분포에서 어느 정도 넓은 구간을 사용할지 정하는 기준이다.

| Band   | 사용하는 분위수 비율                 | 의미               |
| ------ | --------------------------- | ---------------- |
| Narrow | q_low = 0.35, q_high = 0.75 | 비교적 좁은 오차 구간을 사용 |
| Normal | q_low = 0.25, q_high = 0.90 | 일반적인 오차 구간을 사용   |
| Wide   | q_low = 0.10, q_high = 0.95 | 넓은 오차 구간을 사용     |

이 표에서 분위수 범위가 서로 겹쳐 보이는 이유는, 세 band가 서로 다른 데이터를 나누는 구간이 아니기 때문이다. Narrow, Normal, Wide는 “오차 데이터의 어느 범위를 사용할 것인가”를 나타내는 세 가지 신뢰 구간 설정이다. 즉, 하나의 입력 사용자에 대해 세 band를 모두 동시에 사용하는 것이 아니라, 입력 RTT의 안정성에 따라 세 band 중 하나를 선택한다.

band를 선택하기 위해 먼저 한 사용자에 대한 18개 RTT 측정값 벡터 d = [d_hat_1, d_hat_2, ..., d_hat_18]의 상대적 산포를 계산한다.

C = std(d) / (mean(|d|) + delta)

C가 작으면 18개 RTT 값이 상대적으로 안정적이라고 보고 Narrow band를 사용한다. C가 중간이면 Normal band를 사용한다. C가 크면 RTT 입력이 불안정하다고 보고 Wide band를 사용한다.

여기서 cv_low와 cv_high는 band를 선택하기 위한 기준값이다. cv_low와 cv_high는 고리의 안쪽 반지름이나 바깥쪽 반지름이 아니다. 학습 데이터 700개 각각에 대해 C 값을 계산한 뒤, 그 C 값들의 33% 지점과 66% 지점을 cv_low, cv_high로 사용한다.

| 조건                   | 선택되는 band | 의미                            |
| -------------------- | --------- | ----------------------------- |
| C ≤ cv_low           | Narrow    | 입력 RTT가 상대적으로 안정적이므로 좁은 고리 사용 |
| cv_low < C < cv_high | Normal    | 일반적인 입력 상태이므로 기본 고리 사용        |
| C ≥ cv_high          | Wide      | 입력 RTT가 상대적으로 불안정하므로 넓은 고리 사용 |

정리하면, q_low와 q_high는 오차 분위수를 계산하기 위한 비율이고, Q_i^low와 Q_i^high는 앵커별 오차 분포에서 실제로 계산된 오차값이다. cv_low와 cv_high는 어떤 band를 사용할지 고르는 기준이다. 따라서 Q_i^low / Q_i^high와 cv_low / cv_high는 역할이 완전히 다르다.

| 기호                | 의미                            | 역할                                     |
| ----------------- | ----------------------------- | -------------------------------------- |
| q_low, q_high     | 분위수를 계산할 때 사용하는 비율            | Narrow / Normal / Wide band의 폭을 정하는 기준 |
| Q_i^low, Q_i^high | i번째 앵커의 오차 분포에서 계산된 실제 분위수 값  | 고리의 안쪽/바깥쪽 반지름 계산에 사용                  |
| C                 | 한 사용자 입력 RTT의 상대적 산포          | 입력이 안정적인지 불안정한지 판단                     |
| cv_low, cv_high   | 학습 데이터의 C 값에서 계산한 33%, 66% 기준 | Narrow / Normal / Wide 중 하나를 선택하는 기준   |

### 2.4 RTT 측정값의 고리형 신뢰 영역 변환

새로운 사용자에 대해 i번째 앵커의 측정 거리 d_hat_i가 주어졌다고 하자. 기존 삼변측량이나 LS는 사용자가 앵커 i로부터 정확히 d_hat_i만큼 떨어진 원 위에 있다고 본다.

||p - b_i||_2 ≈ d_hat_i

반면 CF-AQARF는 RTT 오차 분위수를 이용하여 실제 거리가 존재할 가능성이 높은 범위를 만든다. i번째 앵커에서 선택된 band의 분위수 결과가 Q_i^low와 Q_i^high라고 하면, 실제 거리의 하한과 상한을 다음과 같이 정의한다.

rho_i^in = d_hat_i - Q_i^high

rho_i^out = d_hat_i - Q_i^low

이렇게 정의하는 이유는 e_i = d_hat_i - 실제 거리이기 때문이다. 만약 측정 오차가 Q_i^high 정도로 크다면 실제 거리는 d_hat_i - Q_i^high 정도로 작아진다. 반대로 측정 오차가 Q_i^low 정도라면 실제 거리는 d_hat_i - Q_i^low 정도가 된다. 따라서 실제 거리는 rho_i^in과 rho_i^out 사이에 있을 가능성이 높다고 볼 수 있다.

rho_i^in ≤ ||p - b_i||_2 ≤ rho_i^out

이 조건은 2차원 평면에서 두께를 가진 고리형 영역을 만든다. 고리의 폭은 다음과 같이 정해진다.

고리 폭 = rho_i^out - rho_i^in = Q_i^high - Q_i^low

따라서 고리의 폭은 임의로 고정된 값이 아니라, 해당 앵커의 오차 분위수 차이로 결정된다. 안정적인 앵커는 Q_i^high - Q_i^low가 작아 좁은 고리를 만들고, 불안정한 앵커는 이 값이 커져 넓은 고리를 만든다. 또한 입력 RTT가 불안정하여 Wide band가 선택되면 q_low와 q_high의 간격이 넓어지므로 전체적으로 더 넓은 고리 영역이 만들어진다.

예를 들어 어떤 앵커의 RTT 측정값이 50 m이고, 학습 데이터에서 해당 앵커의 오차 분위수가 Q_i^low = 8 m, Q_i^high = 20 m라고 하자. 그러면 실제 거리는 대략 30 m에서 42 m 사이에 있을 가능성이 높다고 해석한다. 즉, 반지름 50 m짜리 하나의 원이 아니라 반지름 30 m에서 42 m 사이의 고리 영역을 만드는 것이다.

### 2.5 공간 후보점에 대한 신뢰도장 생성

CF-AQARF는 최종 좌표를 바로 계산하지 않는다. 먼저 2차원 공간 전체에 후보점을 격자 형태로 배치한다. 각 후보 위치 p에 대해 18개 앵커까지의 거리를 계산하고, 해당 후보 위치가 각 앵커의 고리형 신뢰 영역 안에 들어가는지를 평가한다.

후보 위치 p와 i번째 앵커 사이의 거리는 다음과 같다.

r_i(p) = ||p - b_i||_2

후보 위치가 rho_i^in과 rho_i^out 사이에 있으면 해당 앵커의 측정값과 잘 맞는 위치로 보고 높은 점수를 부여한다. 반대로 고리보다 너무 안쪽 또는 바깥쪽에 있으면 점수를 낮춘다.

제공 데이터에서 RTT 측정값은 실제 거리보다 크게 나오는 경향이 있었기 때문에, 고리형 신뢰 영역의 안쪽 위반과 바깥쪽 위반을 비대칭적으로 처리하였다. 후보 위치가 고리 안쪽으로 벗어나는 경우는 RTT 과대추정으로 설명될 수 있으므로 완만하게 감점하였고, 고리 바깥쪽으로 벗어나는 경우는 더 강하게 감점하였다. 최종 모델에서는 이를 반영하기 위해 안쪽 penalty 폭은 확대하고, 바깥쪽 penalty 폭은 축소하였다.

각 앵커가 후보 위치 p에 부여하는 신뢰 점수를 g_i(p)라고 하면, 전체 신뢰도장은 다음과 같이 정의된다.

S(p) = {sum_{i=1}^{18} w_i g_i(p)} / {sum_{i=1}^{18} w_i}

여기서 w_i는 i번째 앵커의 기본 신뢰도이다. 학습 데이터에서 RTT 오차의 표준편차가 작은 앵커는 더 안정적인 앵커로 보고 큰 가중치를 부여하였다.

w_i ∝ 1 / (sigma_e,i + delta)

즉, CF-AQARF는 2차원 맵 전체에 후보 점을 만들고, 각 점이 18개 앵커의 고리형 신뢰 영역을 얼마나 잘 만족하는지 점수를 매긴다. 최종 위치는 이 점수장이 가장 높게 형성되는 영역에서 결정된다.

### 2.6 Coarse-to-Fine 신뢰도장 탐색

2차원 공간 전체를 매우 촘촘하게 탐색하면 계산량이 커지고, 반대로 너무 성긴 격자를 사용하면 위치 정밀도가 떨어진다. 이를 해결하기 위해 CF-AQARF는 coarse-to-fine 탐색 구조를 사용하였다.

먼저 전체 탐색 영역을 coarse step으로 탐색하여 신뢰도가 높은 대략적인 영역을 찾는다. 이후 해당 영역 주변만 fine step으로 다시 탐색하여 위치를 정밀하게 계산한다.

| 단계            | 역할                          |
| ------------- | --------------------------- |
| Coarse search | 전체 공간에서 신뢰도가 높은 대략적인 영역 탐색  |
| Fine search   | coarse 결과 주변을 더 촘촘한 격자로 재탐색 |
| Soft centroid | 상위 신뢰 영역의 중심을 최종 위치로 계산     |

최종 위치는 단순히 신뢰도장이 가장 큰 한 격자점을 선택하지 않고, 상위 신뢰 영역의 soft centroid로 계산하였다.

p_hat = {sum_{p in Omega_top} p exp(beta S(p))} / {sum_{p in Omega_top} exp(beta S(p))}

여기서 Omega_top은 신뢰도 점수가 높은 상위 후보 영역이고, beta는 높은 점수 후보에 얼마나 큰 비중을 둘지 결정하는 집중도 계수이다. 이 방식은 단일 argmax보다 격자 해상도와 잡음에 덜 민감하다.

### 2.7 train.py, model.npz, main.py의 역할

train.py는 제공된 700개 labeled data를 이용하여 CF-AQARF에 필요한 통계 파라미터를 계산한다. 여기서 학습되는 것은 딥러닝이나 Random Forest의 가중치가 아니라, 앵커별 RTT 오차 분위수, 앵커별 신뢰도, 고리 경계의 부드러움, adaptive band 기준, 탐색 영역과 탐색 해상도이다. 학습 결과는 model.npz에 저장된다.

main.py는 채점기가 hidden test set으로 실행하는 최종 추론 파일이다. main.py는 d_hat, BS_positions, model.npz만 사용하여 사용자 위치를 예측하고, 최종적으로 p_hat을 반환한다. 여기서 p_hat은 모든 사용자에 대한 예측 위치 배열이며 크기는 (2, num_user)이다. 첫 번째 행은 예측 x 좌표, 두 번째 행은 예측 y 좌표이다.

| 파일        | 역할                                              | 정답 위치 사용 여부    |
| --------- | ----------------------------------------------- | -------------- |
| train.py  | 제공된 700개 데이터로 분위수와 신뢰도 파라미터 계산, 5-fold 자체 검증 수행 | 사용함            |
| model.npz | 학습된 CF-AQARF 파라미터 저장                            | 정답 자체는 저장하지 않음 |
| main.py   | hidden test의 d_hat과 BS_positions로 p_hat 반환      | 사용하지 않음        |

### 2.8 최종 알고리즘 요약

| 단계 | 내용                                                     |
| -- | ------------------------------------------------------ |
| 1  | 학습 데이터에서 앵커별 실제 거리와 RTT 오차 계산                          |
| 2  | 앵커별 RTT 오차 분위수와 오차 분산 기반 신뢰도 계산                        |
| 3  | 새 입력 RTT의 산포 C를 계산하여 narrow, normal, wide band 중 하나 선택 |
| 4  | 선택된 band의 Q_i^low, Q_i^high로 각 앵커의 고리형 신뢰 영역 생성        |
| 5  | 2차원 후보 공간의 각 점에 대해 18개 앵커의 신뢰 점수 계산                    |
| 6  | 앵커별 신뢰 점수를 누적하여 공간 신뢰도장 S(p) 생성                        |
| 7  | coarse search로 유력 영역을 찾고 fine search로 정밀 탐색            |
| 8  | 상위 신뢰 영역의 soft centroid를 최종 위치 p_hat으로 반환              |

## 3. Agent AI 활용 방안

본 프로젝트에서는 ChatGPT를 활용하였다. AI는 최종 알고리즘을 자동으로 결정하는 역할이 아니라, 본인이 수행한 데이터 분석 결과와 실험 결과를 바탕으로 가능한 알고리즘 후보를 정리하고, 수식·코드 구조·보고서 표현을 검토하는 보조 도구로 사용하였다.

중간발표 단계에서는 본인이 Random Forest 기반 접근을 사용하여 RTT 측정값의 비선형적인 오차 구조를 학습하는 방향을 실험하였다. 이후 실험 결과와 데이터 분석을 통해 Random Forest 직접 회귀 방식의 한계를 확인하였다. Random Forest는 입력 거리값과 위치 사이의 복잡한 관계를 학습할 수 있지만, 위치를 직접 예측하는 방식은 RTT 측정값의 물리적 의미와 앵커별 거리 제약을 충분히 설명하지 못한다고 판단하였다.

본인은 이 한계를 바탕으로, 동시에 검토하던 확률맵 기반 위치 추정 방향으로 알고리즘을 전환하였다. 특히 RTT 측정값이 실제 거리보다 크게 측정되는 경향이 있다는 점에 착안하여, 각 앵커의 측정값을 단일 원이 아닌 고리형 신뢰 영역으로 해석하는 아이디어를 제안하였다. 이 과정에서 본인은 “좌표를 바로 회귀하는 모델”이 아니라 “위치 후보 공간 전체에 점수를 매기는 모델”이 본 데이터셋에 더 적합하다고 판단하였다. 또한 앵커별 오차 분포가 다르다는 점을 반영하기 위해 평균 보정값이 아니라 분위수 구간을 사용하는 방향을 설계하였다.

AI는 이후 단계에서 이 아이디어를 구체화하는 데 활용되었다. 구체적으로 narrow, normal, wide의 적응형 분위수 선택, coarse-to-fine 탐색 구조, soft centroid 기반 최종 위치 결정, 비대칭 penalty 검토, train.py와 main.py의 제출 규격 정리 과정에서 도움을 받았다. 또한 관련 연구를 조사하여 Wi-Fi RTT 오차 모델을 위치 후보의 likelihood에 반영하는 연구와, 분위수 기반 prediction interval 연구가 본 프로젝트의 아이디어와 어떤 점에서 연결되는지 정리하는 데 활용하였다. 다만 최종적으로 어떤 연구를 참고문헌으로 채택할지, 그리고 해당 연구와 본 프로젝트의 차이를 어떻게 설명할지는 본인이 알고리즘 구조와 실험 흐름에 맞추어 결정하였다.

최종 알고리즘 선택 역시 본인이 수행한 실험 결과와 데이터 특성 분석을 바탕으로 결정하였다. WLS 보정이나 Random Forest 잔차 보정도 후보로 검토하였지만, 이 방식들은 최종적으로 거리 잔차 또는 좌표 회귀에 의존하는 성격이 강하다고 판단하였다. 따라서 최종 알고리즘은 RTT의 양의 바이어스를 직접 반영하는 고리형 신뢰도장 방식으로 확정하였다.

| 활용 단계   | 본인이 수행한 부분                                | AI가 보조한 부분                                     |
| ------- | ----------------------------------------- | ---------------------------------------------- |
| 데이터 분석  | RTT 오차가 양의 바이어스와 이상치를 갖는다는 점 확인           | 분석 결과를 알고리즘 요구사항으로 정리                          |
| 중간발표 접근 | Random Forest 기반 실험 수행                    | Random Forest 직접 회귀의 한계 정리                     |
| 알고리즘 전환 | 확률맵 기반 위치 추정 방향 선택, 고리형 신뢰 영역 아이디어 제안     | 확률맵을 신뢰도장 구조로 정리                               |
| 핵심 아이디어 | RTT 측정값을 분위수 기반 고리형 신뢰 영역으로 해석하는 방향 설계    | adaptive band, coarse-to-fine, 비대칭 penalty 구조화 |
| 참고문헌 조사 | 참고문헌의 적합성 판단 및 본 프로젝트와의 차이 정리             | 관련 논문 탐색, 참고한 부분과 차별점 정리 보조                    |
| 코드 구현   | train.py, main.py 실행 및 결과 확인, 최종 제출 구조 점검 | 코드 구조, 제출 규격, 파라미터 검토 보조                       |
| 최종 판단   | 여러 알고리즘 비교분석 및 CF-AQARF를 최종 알고리즘으로 선택                    | 결과 해석과 보고서 문장 정리 보조                            |

## 4. 결과 도출 & 디스커션

### 4.1 평가 방식

제공된 700개 데이터에 대해 두 가지 방식으로 성능을 확인하였다. 첫 번째는 5-fold cross validation이다. 각 fold에서 560개 샘플은 CF-AQARF 파라미터 계산에 사용하고, 나머지 140개 샘플은 검증에 사용하였다. 앵커별 오차 분위수와 신뢰도는 각 fold의 train set에서만 계산하고, validation set의 정답 위치는 파라미터 계산에 사용하지 않았다.

두 번째는 최종 제출 구조와 동일하게 제공된 700개 전체 labeled data로 최종 model.npz를 생성한 뒤, 같은 700개 데이터에 대해 위치 추정을 수행한 결과이다. 이 값은 hidden test 공식 성능을 의미하지는 않지만, 최종 model.npz가 제공 데이터 전체에서 어느 정도의 오차 분포를 보이는지 확인하기 위한 참고 성능이다. 최종 보고서의 대표 성능은 이 전체 700개 재평가 결과를 기준으로 제시하였다.

또한 최종 알고리즘을 선정하는 과정에서는 RF 위치 회귀, QARF, AQARF, CF-QARF, CF-AQARF, WLS 후처리 구조를 순차적으로 검토하였다. 최종 제출용 train.py에는 CF-AQARF의 최종 학습 로직만 포함하였지만, 알고리즘 선정 과정에서는 여러 후보를 비교하였다. 따라서 본 절에서는 최종 코드에 포함된 알고리즘뿐 아니라, 개발 과정에서 검토했던 후보들의 역할과 제외 이유도 함께 정리한다. 

| 항목            | 설정                                      |
| ------------- | --------------------------------------- |
| 전체 제공 데이터 수   | 700                                     |
| 최종 학습 데이터     | 제공된 700개 labeled data 전체                |
| 보조 검증 방식      | 5-fold cross validation                 |
| 각 fold 학습 데이터 | 560                                     |
| 각 fold 검증 데이터 | 140                                     |
| 평가 오차         | 예측 위치 p_hat과 정답 위치 p 사이의 유클리드 거리        |
| 비교 baseline   | 최근접 6개 앵커 기반 Linear LS                  |
| 중간 비교군        | RF 위치 회귀                                |
| 구조 검토         | QARF, AQARF, CF-QARF, CF-AQARF, WLS 후처리 |
| 제안 알고리즘       | CF-AQARF                                |

### 4.2 알고리즘 발전 과정 정량 비교

최종 알고리즘을 선정하기 전, RF 위치 회귀와 QARF 계열 알고리즘을 순차적으로 비교하였다. 이 비교의 목적은 단순히 어떤 모델의 수치가 가장 좋은지를 확인하는 것이 아니라, 본 데이터셋에서 어떤 구조가 RTT 오차의 특성을 가장 잘 반영하는지를 확인하는 것이다.

개발 과정에서 비교한 후보 알고리즘의 대표 성능은 다음과 같다. CF-AQARF는 adaptive band, coarse-to-fine 탐색, 비대칭 penalty를 반영한 성능이다.

| 방법       | MAE [m] | RMSE [m] | 최소 오차 [m] | 최대 오차 [m] | Median [m] | P95 [m] |
| -------- | ------: | -------: | --------: | --------: | ---------: | ------: |
| RF 위치 회귀 |   8.388 |   10.010 |     0.348 |    52.547 |      7.439 |  18.125 |
| QARF     |   5.188 |    7.305 |     0.178 |    58.683 |      3.656 |  14.422 |
| AQARF    |   5.474 |    7.200 |     0.228 |    40.738 |      4.123 |  14.216 |
| CF-QARF  |   4.724 |    7.428 |     0.075 |    61.886 |      2.665 |  15.850 |
| CF-AQARF |   3.349 |    5.032 |     0.113 |    35.411 |      2.010 |  10.496 |

비교 결과, RF 위치 회귀는 RTT 입력과 위치 사이의 비선형 관계를 일부 학습할 수 있었지만, 위치를 직접 회귀하는 구조이므로 물리적 설명성이 부족하였다. 또한 Median error가 7.439 m로 높게 나타나 일반적인 샘플에서 안정적인 위치 추정 성능을 보였다고 보기 어려웠다.

QARF는 RTT 측정값을 단일 거리 반지름으로 보지 않고, 앵커별 오차 분위수를 이용한 고리형 신뢰 영역으로 변환함으로써 RF보다 평균 오차를 크게 낮추었다. 이는 본 데이터셋에서 RTT 양의 바이어스를 단순 보정값이 아니라 공간 신뢰 영역으로 해석하는 방향이 유효하다는 것을 보여준다.

AQARF는 입력 RTT의 상대적 산포에 따라 narrow, normal, wide band를 선택하는 구조를 추가하였다. 이 방식은 입력 상태에 따라 고리 폭을 다르게 적용할 수 있다는 장점이 있었지만, 평균 오차 측면에서는 QARF보다 뚜렷한 개선을 보이지 않았다. 다만 최대 오차가 QARF보다 감소하여, 입력 안정성에 따른 band 선택이 일부 극단 오차 완화에는 의미가 있음을 확인하였다.

CF-QARF는 coarse-to-fine 탐색을 적용하여 MAE와 Median error를 낮추었다. 이는 전체 공간을 한 번에 촘촘히 탐색하는 대신, 먼저 유력 영역을 찾고 그 주변을 정밀 탐색하는 방식이 일반적인 샘플의 위치 정확도를 높이는 데 효과가 있음을 보여준다. 그러나 최대 오차가 61.886 m로 커지는 문제가 있었으며, 이는 coarse 단계에서 잘못된 영역을 선택할 경우 큰 오차가 발생할 수 있음을 의미한다.

최종 CF-AQARF는 adaptive band와 coarse-to-fine 탐색을 결합하고, RTT 양의 바이어스를 반영한 비대칭 penalty를 추가한 구조이다. 후보 위치가 고리 안쪽으로 벗어나는 경우는 RTT 과대추정으로 설명될 수 있으므로 완만하게 감점하고, 고리 바깥쪽으로 벗어나는 경우는 더 강하게 감점하였다. 그 결과 CF-AQARF는 MAE, RMSE, Median, P95, 최대 오차 측면에서 가장 균형 잡힌 결과를 보였다. 따라서 최종 알고리즘은 CF-AQARF로 선정하였다.

### 4.3 최종 모델의 700개 전체 평가 결과

제공된 700개 전체 labeled data를 이용하여 최종 CF-AQARF 파라미터를 계산하고, 같은 700개 데이터에 대해 위치 추정을 수행하였다. 이 결과는 hidden test 공식 성능이 아니라, 최종 제출용 model.npz가 제공 데이터 전체에서 보이는 in-sample 성능이다.

최종 성능 비교에서는 거리 방정식 기반 baseline인 Linear LS K=6과 최종 CF-AQARF를 비교하였다. Linear LS는 RTT 측정값을 실제 거리로 사용하는 기본 위치 추정 방식이므로, 본 프로젝트가 해결하려는 거리 과대추정과 이상치 문제를 보여주는 기준선 역할을 한다.

| Method        | MAE [m] | RMSE [m] | Min [m] | Max [m] | Median [m] | P95 [m] |
| ------------- | ------: | -------: | ------: | ------: | ---------: | ------: |
| Linear LS K=6 |   8.311 |   11.332 |   0.055 |  61.656 |      5.898 |  23.718 |
| CF-AQARF      |   3.349 |    5.032 |   0.113 |  35.411 |      2.010 |  10.496 |

CF-AQARF는 Linear LS K=6 대비 모든 주요 지표에서 개선되었다. MAE는 8.311 m에서 3.349 m로 감소하였고, RMSE는 11.332 m에서 5.032 m로 감소하였다. Median error도 5.898 m에서 2.010 m로 감소하였으며, P95 error는 23.718 m에서 10.496 m로 감소하였다.

| Metric       | Linear LS K=6 | CF-AQARF | Reduction |
| ------------ | ------------: | -------: | --------: |
| MAE          |       8.311 m |  3.349 m |     59.7% |
| RMSE         |      11.332 m |  5.032 m |     55.6% |
| Median Error |       5.898 m |  2.010 m |     65.9% |
| P95 Error    |      23.718 m | 10.496 m |     55.7% |
| Max Error    |      61.656 m | 35.411 m |     42.6% |

이 결과는 RTT 측정값을 단일 거리 반지름으로 사용하는 LS 방식보다, 앵커별 오차 분위수와 고리형 신뢰 영역을 사용하는 방식이 본 데이터셋에 더 적합함을 보여준다. 특히 Median error가 2.010 m로 감소한 것은 일반적인 샘플에서 CF-AQARF가 안정적인 위치 추정 성능을 보였다는 의미이다.

### 4.4 5-fold 보조 검증 결과

전체 700개 재평가 결과만으로 성능을 판단하면 제공 데이터에 대한 과적합 가능성을 확인하기 어렵다. 따라서 보조적으로 5-fold cross validation을 수행하였다. 각 fold의 validation set은 해당 fold의 분위수 계산과 신뢰도 파라미터 계산에 사용하지 않았다.

| Fold | CF-AQARF MAE [m] | CF-AQARF RMSE [m] | CF-AQARF P95 [m] | CF-AQARF Max [m] |
| ---: | ---------------: | ----------------: | ---------------: | ---------------: |
|    1 |            3.417 |             5.464 |           10.540 |           35.195 |
|    2 |            3.158 |             4.506 |            9.719 |           21.577 |
|    3 |            3.450 |             5.332 |           12.441 |           25.183 |
|    4 |            3.577 |             5.062 |            9.743 |           20.197 |
|    5 |            3.291 |             4.944 |           10.676 |           21.148 |

5-fold 전체를 합친 결과는 다음과 같다.

| Method          | MAE [m] | RMSE [m] | Min [m] | Max [m] | Median [m] | P95 [m] |
| --------------- | ------: | -------: | ------: | ------: | ---------: | ------: |
| CF-AQARF 5-fold |   3.378 |    5.073 |   0.084 |  35.195 |      2.017 |  10.482 |

5-fold 결과와 700개 전체 재평가 결과의 차이는 크지 않았다. 700개 전체 재평가에서 MAE는 3.349 m이고, 5-fold 결과에서 MAE는 3.378 m로 나타났다. 이는 최종 모델이 단순히 학습 데이터에만 과도하게 맞춰져 성능이 급격히 좋아진 것이 아니라, fold가 나뉜 상황에서도 비슷한 수준의 성능을 유지했음을 의미한다.

| 평가 방식               | MAE [m] | RMSE [m] | Median [m] | P95 [m] |
| ------------------- | ------: | -------: | ---------: | ------: |
| 700개 전체 학습 후 전체 재평가 |   3.349 |    5.032 |      2.010 |  10.496 |
| 5-fold validation   |   3.378 |    5.073 |      2.017 |  10.482 |

### 4.5 Baseline 비교의 fairness

공지에서는 딥러닝 모델과 단순 삼변측량을 단순 수치로만 비교하는 것이 공정하지 않을 수 있다고 설명하였다. 본 프로젝트의 CF-AQARF는 딥러닝은 아니지만, labeled data에서 앵커별 RTT 오차 분위수와 신뢰도 파라미터를 학습하여 사용하는 hybrid learning-based 방식이다. 따라서 Linear LS와 CF-AQARF를 단순히 같은 수준의 알고리즘으로 놓고 “성능이 더 좋다”고만 주장하는 것은 충분하지 않다.

본 보고서에서 Linear LS K=6을 사용한 목적은 최종 알고리즘과 완전히 대등한 학습 기반 비교군을 만들기 위한 것이 아니라, RTT 측정값을 실제 거리로 그대로 사용하는 기본 거리 방정식 방식이 본 데이터에서 어느 정도 한계를 가지는지 확인하기 위한 baseline을 제시하는 것이다. 즉, Linear LS는 “학습 기반 모델과의 완전한 공정 비교 대상”이라기보다는, 본 프로젝트가 해결하려는 거리 과대추정과 이상치 문제를 보여주는 기준선 역할을 한다.

공정성을 보완하기 위해 본 프로젝트에서는 세 가지 비교를 함께 제시하였다. 첫째, Linear LS를 통해 거리 방정식 기반 baseline의 한계를 확인하였다. 둘째, RF 위치 회귀를 검토하여 학습 기반 좌표 회귀 방식의 가능성과 한계를 확인하였다. 셋째, QARF, AQARF, CF-QARF, CF-AQARF의 정량 비교를 통해 최종 알고리즘이 단순히 LS보다 성능이 좋은 모델이 아니라, 데이터 특성을 단계적으로 반영한 결과임을 설명하였다.

두 방법 모두 최종 위치 추정 입력으로는 동일한 d_hat과 BS_positions만 사용한다. CF-AQARF는 추가 센서나 외부 정보를 사용하지 않고, 제공된 700개 labeled data에서 계산한 앵커별 RTT 오차 분위수와 신뢰도 파라미터를 model.npz에 저장하여 hidden test에서 사용한다. 또한 5-fold validation에서는 각 fold의 validation 정답 위치를 분위수 계산에 사용하지 않았으므로, validation 성능 비교에서 정답 정보 누수를 피하였다.

### 4.6 디스커션

CF-AQARF의 가장 큰 장점은 RTT 오차의 양의 바이어스를 알고리즘 구조에 직접 반영한다는 점이다. 기존 LS는 측정 거리 d_hat_i를 그대로 실제 거리 반지름으로 사용하지만, CF-AQARF는 학습 데이터에서 계산한 오차 분위수를 이용하여 실제 거리가 존재할 가능성이 높은 고리형 영역을 구성한다. 이로 인해 특정 RTT 측정값이 실제 거리보다 크게 측정되더라도, 알고리즘이 그 값을 무조건 만족해야 하는 거리 제약으로 취급하지 않는다.

또한 CF-AQARF는 최종 위치를 하나의 잔차 제곱합 최소화 문제로 계산하지 않는다. 대신 2차원 공간 전체에 대해 각 후보점의 신뢰도를 계산하고, 여러 앵커가 동시에 지지하는 상위 신뢰 영역의 중심을 최종 위치로 사용한다. 이 방식은 직접 ML 회귀보다 물리적 설명이 가능하고, WLS보다 RTT 오차의 불확실성을 더 명시적으로 표현할 수 있다.

본 프로젝트에서는 고리형 신뢰 영역에서 벗어난 후보 위치를 평가할 때 비대칭 penalty를 사용하였다. RTT 측정값이 실제 거리보다 크게 측정되는 경향이 있었기 때문에, 후보 위치가 고리 안쪽으로 벗어나는 경우는 상대적으로 완만하게 감점하고, 고리 바깥쪽으로 벗어나는 경우는 더 강하게 감점하였다. 이 수정은 단순한 성능 튜닝이 아니라, 데이터 분석에서 확인한 RTT 양의 바이어스를 위치 후보 점수화 과정에 반영한 것이다.

한계도 존재한다. 첫째, CF-AQARF는 격자 기반 탐색을 사용하므로 탐색 범위와 격자 해상도에 영향을 받는다. 본 프로젝트에서는 coarse-to-fine 구조를 사용하여 계산량과 정밀도 사이의 균형을 맞추었지만, hidden test set의 위치 분포가 제공 데이터보다 넓을 경우 탐색 영역 설정이 성능에 영향을 줄 수 있다. 둘째, 신뢰도장이 여러 개의 봉우리로 나뉘는 경우 soft centroid가 중간 위치로 이동할 가능성이 있다. 셋째, 최대 오차는 baseline보다 줄었지만 여전히 35 m 이상의 극단 오차가 존재하므로 일부 극단 샘플에 대한 추가적인 안정화가 필요하다.

향후 개선 방향은 세 가지이다. 첫째, 신뢰도장이 여러 봉우리로 형성되는 경우를 감지하여 단일 soft centroid 대신 peak별 후보를 비교하는 구조를 추가할 수 있다. 둘째, 앵커별 분위수뿐 아니라 거리 구간별 분위수를 함께 사용하면 가까운 거리와 먼 거리에서의 오차 차이를 더 정밀하게 반영할 수 있다. 셋째, CF-AQARF 결과의 극단 오차 샘플만을 대상으로 별도의 residual correction을 적용하면 평균 성능을 유지하면서 최대 오차를 줄일 수 있을 것으로 기대된다.


## 5. Reference

본 프로젝트는 Wi-Fi RTT 측정값을 그대로 실제 거리로 사용하지 않고, 측정 오차의 불확실성을 위치 추정 과정에 반영한다는 점에서 Gentner et al.의 WiFi-RTT Indoor Positioning 연구를 참고하였다. 해당 연구는 Wi-Fi RTT distance error model을 구성하고 이를 particle filter의 likelihood function에 포함하여 후보 위치의 가능도를 평가하였다. 본 프로젝트 역시 RTT 측정값의 오차 특성을 위치 후보의 신뢰도 계산에 반영한다는 관점은 동일하지만, particle filter나 시간적 필터링을 사용하지 않고 단일 시점의 2차원 후보 공간에서 앵커별 고리형 신뢰 영역을 누적하는 방식으로 변형하였다.

또한 분위수를 이용해 단일 점 예측이 아닌 구간 형태로 불확실성을 표현하는 관점은 Pouplin et al.의 quantile 기반 prediction interval 연구를 참고하였다. 해당 연구는 참값이 포함될 가능성이 높은 예측 구간을 구성하는 것이 불확실성 표현에 중요하다고 설명한다. 본 프로젝트는 quantile regression 모델을 학습하지는 않았지만, labeled data에서 앵커별 RTT 오차의 empirical quantile을 계산하고, 이를 실제 거리가 존재할 가능성이 높은 안쪽 반지름과 바깥쪽 반지름으로 변환하였다. 즉, 기존 연구의 prediction interval 개념을 RTT 기반 위치 추정 문제에 맞게 고리형 신뢰 영역으로 재구성한 것이 본 프로젝트의 차별점이다.

| Reference                                                                                               | 참고한 내용                                                      | 본 프로젝트와의 차이                                                                       |
| ------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------- | --------------------------------------------------------------------------------- |
| C. Gentner et al., “WiFi-RTT Indoor Positioning,” IEEE/ION PLANS, 2020                                  | Wi-Fi RTT 거리 오차 모델을 likelihood function에 반영하여 후보 위치 가능도를 평가 | 본 프로젝트는 particle filter 대신 2D 격자 기반 고리형 신뢰도장을 사용                                  |
| T. Pouplin et al., “Relaxed Quantile Regression: Prediction Intervals for Asymmetric Noise,” ICML, 2024 | 단일 점 예측 대신 quantile 기반 prediction interval로 불확실성 표현         | 본 프로젝트는 quantile regression이 아니라 앵커별 empirical RTT error quantile을 고리형 거리 구간으로 사용 |
