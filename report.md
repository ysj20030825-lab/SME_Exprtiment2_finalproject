# 스마트모빌리티공학실험2 Final Project Report

## 1. 모티베이션 & 인트로

본 프로젝트의 목표는 18개 Wi-Fi RTT 앵커가 측정한 거리값과 앵커 좌표를 이용하여 사용자의 2차원 위치를 추정하는 것이다. 제공 데이터에는 전체 사용자 1000명 중 700명의 정답 위치가 포함되어 있으며, 나머지 300명은 hidden test set으로 사용된다. 따라서 최종 알고리즘은 제공된 700개 데이터에만 과도하게 맞춰지는 것이 아니라, 보이지 않는 데이터에서도 안정적으로 동작해야 한다.

초기에는 가장 기본적인 거리 기반 위치 추정 방법인 삼변측량, Linear Least Squares, Weighted Least Squares 계열 방법을 검토하였다. 이러한 방식은 각 앵커의 RTT 측정값을 실제 거리로 보고, 여러 거리 방정식을 가장 잘 만족하는 위치를 찾는다. 그러나 데이터 분석 결과, RTT 측정값은 실제 거리보다 크게 측정되는 양의 바이어스가 강하게 나타났다. 또한 일부 앵커에서는 큰 이상치가 포함되어 있었고, 앵커별 오차 분포도 서로 다르게 나타났다. 이 때문에 모든 앵커의 거리값을 하나의 정확한 반지름처럼 사용하는 방식은 최종 위치 추정에 한계가 있다고 판단하였다.

중간발표 단계에서는 Random Forest를 이용하여 RTT 측정값의 비선형적인 오차 구조를 학습하는 방향을 실험하였다. Random Forest는 단순 선형 모델보다 복잡한 오차 패턴을 반영할 수 있다는 장점이 있었다. 그러나 실험을 진행하면서 Random Forest가 위치를 직접 예측하는 방식은 두 가지 한계를 가진다고 판단하였다. 첫째, 최종 위치를 데이터 기반으로 바로 회귀하기 때문에 앵커와 사용자 사이의 물리적 거리 제약을 충분히 설명하기 어렵다. 둘째, 제공된 700개 데이터의 위치 분포에 과적합될 가능성이 있어 hidden test set에서 안정성이 떨어질 수 있다.

이에 따라 본인은 Random Forest 기반 직접 회귀에서 더 나아가, 이전에 함께 검토했던 확률맵 기반 위치 추정 방향을 다시 고려하였다. 단순히 좌표를 직접 예측하는 대신, 2차원 공간 전체에 대해 “각 위치가 얼마나 그럴듯한가”를 점수화하는 방식이 본 데이터셋에 더 적합하다고 판단하였다. 특히 RTT 측정값이 실제 거리보다 크게 측정되는 경향이 있다는 점에 착안하여, 각 앵커의 측정값을 하나의 원 반지름이 아니라 실제 거리가 존재할 가능성이 높은 범위로 해석하는 고리형 신뢰 영역을 제안하였다.

최종 알고리즘은 CF-AQARF, 즉 Coarse-to-Fine Adaptive Quantile Annulus Reliability Field로 정의하였다. 한국어로는 Coarse-to-Fine 적응형 분위수 고리형 신뢰도장 위치 추정 알고리즘이다. 이 알고리즘은 RTT 측정값을 단일 거리 원으로 보지 않고, 학습 데이터에서 계산한 앵커별 RTT 오차 분위수를 이용하여 두께를 가진 고리형 신뢰 영역으로 변환한다. 이후 18개 앵커의 고리형 신뢰 영역을 2차원 공간에 누적하여 신뢰도장을 만들고, 신뢰도가 높은 영역의 중심을 최종 위치로 산출한다.

| 구분                  | 기존 접근                   | 한계                     | 본 프로젝트의 접근                             |
| ------------------- | ----------------------- | ---------------------- | -------------------------------------- |
| Linear LS           | RTT 측정값을 실제 거리 반지름으로 사용 | 양의 바이어스와 이상치에 취약       | RTT 오차 분위수로 거리 범위 생성                   |
| WLS / IRWLS         | 앵커별 가중치로 거리 잔차 최소화      | 최종적으로는 거리 잔차 최소화에 의존   | 잔차 최소화 대신 공간 신뢰도장 생성                   |
| Random Forest 직접 회귀 | RTT에서 x, y 좌표 직접 예측     | 물리적 거리 제약 반영이 약함       | 직접 회귀 대신 앵커별 신뢰 영역 누적                  |
| CF-AQARF            | 고리형 신뢰 영역 기반 공간 점수화     | 격자 해상도와 일부 극단 샘플 영향 존재 | adaptive band와 coarse-to-fine 탐색으로 안정화 |

## 2. 알고리즘 설명

### 2.1 실제 거리와 RTT 오차 정의

i번째 앵커의 위치를 (b_i=[x_i,y_i]^T), u번째 사용자의 정답 위치를 (p_u=[x_u,y_u]^T)라고 하면 실제 거리는 다음과 같이 계산된다.

(r_{i,u}=|p_u-b_i|_2=\sqrt{(x_u-x_i)^2+(y_u-y_i)^2})

RTT 측정 거리 (\hat{d}_{i,u})와 실제 거리의 차이를 RTT 오차로 정의한다.

(e_{i,u}=\hat{d}*{i,u}-r*{i,u})

여기서 (e_{i,u}>0)이면 측정 거리가 실제 거리보다 크게 측정되었다는 의미이고, (e_{i,u}<0)이면 측정 거리가 실제 거리보다 작게 측정되었다는 의미이다. 제공 데이터 분석 결과, 양의 오차가 많이 나타났으므로 RTT 측정값을 실제 거리 자체로 보기보다는 실제 거리보다 길게 측정된 값으로 해석하는 것이 더 적합하다고 판단하였다.

### 2.2 분위수의 의미와 앵커별 오차 분포 학습

분위수는 데이터를 작은 값부터 큰 값까지 정렬했을 때 특정 비율에 해당하는 값이다. 예를 들어 0.25 분위수는 전체 데이터 중 약 25%가 그 값보다 작거나 같은 지점이고, 0.90 분위수는 전체 데이터 중 약 90%가 그 값보다 작거나 같은 지점이다. 평균은 큰 이상치에 민감하지만, 분위수는 데이터의 범위와 퍼짐을 표현하는 데 유리하다.

본 알고리즘은 각 앵커별로 RTT 오차 분포를 계산하고, 이 분포에서 하위 분위수와 상위 분위수를 구한다. i번째 앵커에 대해 모든 학습 사용자에 대한 오차 집합을 다음과 같이 둔다.

({e_{i,u}}_{u=1}^{N})

이 오차 분포에서 하위 분위수와 상위 분위수를 계산한다.

(Q_i^{low}=\mathrm{quantile}({e_{i,u}},q_{low}))

(Q_i^{high}=\mathrm{quantile}({e_{i,u}},q_{high}))

본 알고리즘에서는 입력 RTT의 안정성에 따라 세 가지 분위수 범위를 사용하였다.

| Band   | 분위수 범위      | 의미                            |
| ------ | ----------- | ----------------------------- |
| Narrow | 0.35 ~ 0.75 | RTT 입력이 비교적 안정적인 경우, 좁은 고리 사용 |
| Normal | 0.25 ~ 0.90 | 일반적인 경우, 기본 고리 사용             |
| Wide   | 0.10 ~ 0.95 | RTT 입력이 불안정한 경우, 넓은 고리 사용     |

이때 각 band 안에서도 모든 앵커의 고리 폭이 같은 것은 아니다. 분위수는 앵커별로 따로 계산되기 때문에, 같은 Normal band를 사용하더라도 1번 앵커와 2번 앵커의 (Q_i^{low}), (Q_i^{high})는 다를 수 있다. 따라서 CF-AQARF는 전체적으로는 narrow, normal, wide의 세 단계 adaptive 구조를 갖지만, 실제 고리 폭은 앵커별 오차 분포에 따라 다르게 형성된다.

### 2.3 RTT 측정값의 고리형 신뢰 영역 변환

새로운 사용자에 대해 i번째 앵커의 측정 거리 (\hat{d}_i)가 주어졌다고 하자. 기존 삼변측량이나 LS는 사용자가 앵커 i로부터 정확히 (\hat{d}_i)만큼 떨어진 원 위에 있다고 본다.

(|p-b_i|_2\approx\hat{d}_i)

반면 CF-AQARF는 RTT 오차 분위수를 이용하여 실제 거리가 존재할 가능성이 높은 범위를 만든다.

(\rho_i^{in}=\hat{d}_i-Q_i^{high})

(\rho_i^{out}=\hat{d}_i-Q_i^{low})

따라서 i번째 앵커는 하나의 원이 아니라 다음 조건을 만족하는 고리형 신뢰 영역을 제공한다.

(\rho_i^{in}\leq|p-b_i|_2\leq\rho_i^{out})

예를 들어 어떤 앵커의 RTT 측정값이 50 m이고, 학습 데이터에서 해당 앵커의 오차 분위수가 (Q_i^{low}=8) m, (Q_i^{high}=20) m라고 하자. 그러면 실제 거리는 대략 30 m에서 42 m 사이에 있을 가능성이 높다고 해석한다. 즉, 반지름 50 m짜리 하나의 원이 아니라 반지름 30 m에서 42 m 사이의 고리 영역을 만드는 것이다.

이 방식은 본 데이터셋에서 관찰된 RTT 양의 바이어스를 직접 반영한다. 측정값을 정확한 거리로 강제하지 않고, 실제 거리가 존재할 가능성이 높은 범위로 변환한다는 점이 LS 계열 방식과의 핵심적인 차이이다.

### 2.4 공간 후보점에 대한 신뢰도장 생성

CF-AQARF는 최종 좌표를 바로 계산하지 않는다. 먼저 2차원 공간 전체에 후보점을 격자 형태로 배치한다. 각 후보 위치 (p)에 대해 18개 앵커까지의 거리를 계산하고, 해당 후보 위치가 각 앵커의 고리형 신뢰 영역 안에 들어가는지를 평가한다.

후보 위치 (p)와 i번째 앵커 사이의 거리는 다음과 같다.

(r_i(p)=|p-b_i|_2)

후보 위치가 (\rho_i^{in})과 (\rho_i^{out}) 사이에 있으면 해당 앵커의 측정값과 잘 맞는 위치로 보고 높은 점수를 부여한다. 반대로 고리보다 너무 안쪽 또는 바깥쪽에 있으면 점수를 낮춘다. 특히 RTT 측정값은 실제 거리보다 크게 나오는 경향이 있으므로, 고리 바깥쪽으로 벗어난 경우를 더 강하게 감점하고, 고리 안쪽으로 벗어난 경우는 상대적으로 완만하게 감점하였다.

각 앵커가 후보 위치 (p)에 부여하는 신뢰 점수를 (g_i(p))라고 하면, 전체 신뢰도장은 다음과 같이 정의된다.

(S(p)=\frac{\sum_{i=1}^{18}w_i g_i(p)}{\sum_{i=1}^{18}w_i})

여기서 (w_i)는 i번째 앵커의 기본 신뢰도이다. 학습 데이터에서 RTT 오차의 표준편차가 작은 앵커는 더 안정적인 앵커로 보고 큰 가중치를 부여하였다.

(w_i\propto\frac{1}{\sigma_{e,i}+\delta})

즉, CF-AQARF는 2차원 맵 전체에 점을 흩뿌려 놓고, 각 점이 18개 앵커의 고리형 신뢰 영역을 얼마나 잘 만족하는지 점수를 매긴다. 최종 위치는 이 점수장이 가장 높게 형성되는 영역에서 결정된다.

### 2.5 Adaptive Quantile 선택

모든 사용자에게 동일한 분위수 폭을 적용하면, RTT 입력이 안정적인 경우에는 후보 영역이 불필요하게 넓어지고, RTT 입력이 불안정한 경우에는 후보 영역이 너무 좁아질 수 있다. 따라서 CF-AQARF는 입력 RTT의 일관성에 따라 narrow, normal, wide band 중 하나를 선택한다.

한 사용자에 대한 18개 RTT 측정값 벡터를 (d=[\hat{d}_1,\hat{d}*2,\dots,\hat{d}*{18}])라고 할 때, 입력의 상대적 산포를 다음과 같이 계산한다.

(C=\frac{\mathrm{std}(d)}{\mathrm{mean}(|d|)+\delta})

이 값이 작으면 입력 RTT 값들이 비교적 안정적이라고 보고 narrow band를 선택한다. 값이 크면 RTT 값의 산포가 크다고 보고 wide band를 선택한다. `cv_low`와 `cv_high`는 학습 데이터에서 계산한 (C) 값들의 33% 분위수와 66% 분위수이다.

| 조건                     | 선택되는 band | 의미                |
| ---------------------- | --------- | ----------------- |
| (C\leq cv_{low})       | Narrow    | 입력 RTT가 상대적으로 안정적 |
| (cv_{low}<C<cv_{high}) | Normal    | 일반적인 입력 상태        |
| (C\geq cv_{high})      | Wide      | 입력 RTT가 상대적으로 불안정 |

즉, `cv_low`와 `cv_high`는 고리 폭 자체가 아니라, 어떤 분위수 band를 사용할지 결정하는 기준값이다.

### 2.6 Coarse-to-Fine 신뢰도장 탐색

2차원 공간 전체를 매우 촘촘하게 탐색하면 계산량이 커지고, 반대로 너무 성긴 격자를 사용하면 위치 정밀도가 떨어진다. 이를 해결하기 위해 CF-AQARF는 coarse-to-fine 탐색 구조를 사용하였다.

먼저 전체 탐색 영역을 coarse step으로 탐색하여 신뢰도가 높은 대략적인 영역을 찾는다. 이후 해당 영역 주변만 fine step으로 다시 탐색하여 위치를 정밀하게 계산한다.

| 단계            | 역할                          |
| ------------- | --------------------------- |
| Coarse search | 전체 공간에서 신뢰도가 높은 대략적인 영역 탐색  |
| Fine search   | coarse 결과 주변을 더 촘촘한 격자로 재탐색 |
| Soft centroid | 상위 신뢰 영역의 중심을 최종 위치로 계산     |

최종 위치는 단순히 신뢰도장이 가장 큰 한 격자점을 선택하지 않고, 상위 신뢰 영역의 soft centroid로 계산하였다.

(\hat{p}=\frac{\sum_{p\in\Omega_{top}}p\exp(\beta S(p))}{\sum_{p\in\Omega_{top}}\exp(\beta S(p))})

여기서 (\Omega_{top})은 신뢰도 점수가 높은 상위 후보 영역이고, (\beta)는 높은 점수 후보에 얼마나 큰 비중을 둘지 결정하는 집중도 계수이다. 이 방식은 단일 argmax보다 격자 해상도와 잡음에 덜 민감하다.

### 2.7 `train.py`, `model.npz`, `main.py`의 역할

`train.py`는 제공된 700개 labeled data를 이용하여 CF-AQARF에 필요한 통계 파라미터를 계산한다. 여기서 학습되는 것은 딥러닝이나 Random Forest의 가중치가 아니라, 앵커별 RTT 오차 분위수, 앵커별 신뢰도, 고리 경계의 부드러움, adaptive band 기준, 탐색 영역과 탐색 해상도이다. 학습 결과는 `model.npz`에 저장된다.

`main.py`는 채점기가 hidden test set으로 실행하는 최종 추론 파일이다. `main.py`는 `d_hat`, `BS_positions`, `model.npz`만 사용하여 사용자 위치를 예측하고, 최종적으로 (p_{hat})을 반환한다. 여기서 (p_{hat})은 모든 사용자에 대한 예측 위치 배열이며 크기는 `(2, num_user)`이다. 첫 번째 행은 예측 x 좌표, 두 번째 행은 예측 y 좌표이다.

채점기는 `main.py`의 출력 문장을 보는 것이 아니라, `main()` 함수가 반환하는 (p_{hat}) 배열을 받아 조교가 가진 hidden 정답 위치와 비교한다. 따라서 `main.py`가 MAE나 RMSE를 직접 출력하지 않는 것은 정상이다. MAE와 RMSE는 hidden test 정답을 알고 있는 채점기 내부에서 계산된다.

| 파일          | 역할                                                 | 정답 위치 사용 여부    |
| ----------- | -------------------------------------------------- | -------------- |
| `train.py`  | 제공된 700개 데이터로 분위수와 신뢰도 파라미터 계산, 5-fold 자체 검증 수행    | 사용함            |
| `model.npz` | 학습된 CF-AQARF 파라미터 저장                               | 정답 자체는 저장하지 않음 |
| `main.py`   | hidden test의 `d_hat`과 `BS_positions`로 (p_{hat}) 반환 | 사용하지 않음        |

### 2.8 최종 알고리즘 요약

| 단계 | 내용                                                   |
| -- | ---------------------------------------------------- |
| 1  | 학습 데이터에서 앵커별 실제 거리와 RTT 오차 계산                        |
| 2  | 앵커별 RTT 오차 분위수와 오차 분산 기반 신뢰도 계산                      |
| 3  | 새 입력 RTT의 산포를 계산하여 narrow, normal, wide band 중 하나 선택 |
| 4  | 각 앵커의 RTT 측정값을 분위수 기반 고리형 신뢰 영역으로 변환                 |
| 5  | 2차원 후보 공간의 각 점에 대해 18개 앵커의 신뢰 점수 계산                  |
| 6  | 앵커별 신뢰 점수를 누적하여 공간 신뢰도장 (S(p)) 생성                    |
| 7  | coarse search로 유력 영역을 찾고 fine search로 정밀 탐색          |
| 8  | 상위 신뢰 영역의 soft centroid를 최종 위치 (p_{hat})으로 반환        |

## 3. Agent AI 활용 방안

본 프로젝트에서는 ChatGPT를 활용하였다. AI는 최종 알고리즘을 자동으로 결정하는 역할이 아니라, 본인이 수행한 데이터 분석 결과와 실험 결과를 바탕으로 가능한 알고리즘 후보를 비교하고, 수식과 코드 구조를 정리하는 보조 도구로 사용하였다.

중간발표 단계에서는 본인이 Random Forest 기반 접근을 사용하여 RTT 측정값의 비선형적인 오차 구조를 학습하는 방향을 실험하였다. 이후 실험 결과와 데이터 분석을 통해 Random Forest 직접 회귀 방식의 한계를 확인하였다. Random Forest는 입력 거리값과 위치 사이의 복잡한 관계를 학습할 수 있지만, 위치를 직접 예측하는 방식은 RTT 측정값의 물리적 의미와 앵커별 거리 제약을 충분히 설명하지 못한다고 판단하였다.

본인은 이 한계를 바탕으로, 동시에 검토하던 확률맵 기반 위치 추정 방향으로 알고리즘을 전환하였다. 특히 RTT 측정값이 실제 거리보다 크게 측정되는 경향이 있다는 점을 이용하여, 각 앵커의 측정값을 단일 원이 아닌 고리형 신뢰 영역으로 해석하는 아이디어를 제안하였다. 이 고리형 신뢰 영역을 공간 전체에 누적하면 위치 가능성 지도를 만들 수 있고, 이는 기존 LS나 직접 ML 회귀보다 데이터 특성을 더 잘 반영할 수 있다고 판단하였다.

AI는 이후 단계에서 이 아이디어를 구체화하는 데 활용되었다. 구체적으로 narrow, normal, wide의 적응형 분위수 선택, coarse-to-fine 탐색 구조, soft centroid 기반 최종 위치 결정, 파라미터 후보 비교, `train.py`와 `main.py`의 제출 규격 정리 과정에서 도움을 받았다. 또한 최종 보고서에서 알고리즘의 수식과 설명을 자연스럽게 정리하는 데 활용하였다.

다만 최종 알고리즘 선택은 본인이 수행한 실험 결과와 데이터 특성 분석을 바탕으로 결정하였다. WLS 보정이나 Random Forest 잔차 보정도 후보로 검토하였지만, 이 방식들은 다른 학생들도 충분히 사용할 수 있는 흔한 구조이고, 최종적으로는 RTT의 양의 바이어스를 직접 반영하는 고리형 신뢰도장 방식이 더 참신하고 설명 가능성이 높다고 판단하였다.

| 활용 단계   | 본인이 수행한 부분                       | AI가 보조한 부분                          |
| ------- | -------------------------------- | ----------------------------------- |
| 데이터 분석  | RTT 오차가 양의 바이어스와 이상치를 갖는다는 점 확인  | 분석 결과를 알고리즘 요구사항으로 정리               |
| 중간발표 접근 | Random Forest 기반 실험 수행           | Random Forest 직접 회귀의 한계 정리          |
| 알고리즘 전환 | 확률맵 기반 위치 추정 방향 선택               | 확률맵을 신뢰도장 구조로 정리                    |
| 핵심 아이디어 | RTT 측정값을 고리형 신뢰 영역으로 해석하는 방향 제안  | adaptive band와 coarse-to-fine 구조 제안 |
| 코드 구현   | `train.py`, `main.py` 실행 및 결과 확인 | 코드 구조, 제출 규격, 파라미터 비교 보조            |
| 최종 판단   | CF-AQARF를 최종 알고리즘으로 선택           | 결과 해석과 보고서 문장 정리 보조                 |

## 4. 결과 도출 & 디스커션

### 4.1 평가 방식

제공된 700개 데이터에 대해 5-fold cross validation을 수행하였다. 각 fold에서 560개 샘플은 CF-AQARF 파라미터 계산에 사용하고, 나머지 140개 샘플은 검증에 사용하였다. 앵커별 오차 분위수와 신뢰도는 각 fold의 train set에서만 계산하고, validation set의 정답 위치는 파라미터 계산에 사용하지 않았다.

| 항목            | 설정                      |
| ------------- | ----------------------- |
| 전체 제공 데이터 수   | 700                     |
| 검증 방식         | 5-fold cross validation |
| 각 fold 학습 데이터 | 560                     |
| 각 fold 검증 데이터 | 140                     |
| 평가 오차         | (|\hat{p}-p|_2)         |
| 비교 baseline   | 최근접 6개 앵커 기반 Linear LS  |
| 제안 알고리즘       | CF-AQARF                |

### 4.2 Fold별 결과

| Fold | CF-AQARF MAE [m] | CF-AQARF RMSE [m] |
| ---: | ---------------: | ----------------: |
|    1 |            4.055 |             6.075 |
|    2 |            3.577 |             5.231 |
|    3 |            4.150 |             6.921 |
|    4 |            4.416 |             6.315 |
|    5 |            3.892 |             5.693 |

Fold별 성능을 보면 모든 fold에서 MAE가 약 3.6 m에서 4.4 m 사이로 나타났다. 이는 특정 split에서만 성능이 좋은 것이 아니라, 제공 데이터 내부에서 비교적 일관적인 성능을 보였음을 의미한다.

### 4.3 Baseline 비교

| Method        | MAE [m] | RMSE [m] | Min [m] | Max [m] | Median [m] | P95 [m] |
| ------------- | ------: | -------: | ------: | ------: | ---------: | ------: |
| Linear LS K=6 |   8.311 |   11.332 |   0.055 |  61.656 |      5.898 |  23.718 |
| CF-AQARF      |   4.018 |    6.074 |   0.046 |  37.877 |      2.439 |  12.992 |

CF-AQARF는 Linear LS K=6 대비 모든 주요 지표에서 개선되었다. MAE는 8.311 m에서 4.018 m로 감소하였고, RMSE는 11.332 m에서 6.074 m로 감소하였다. Median error도 5.898 m에서 2.439 m로 감소하였으며, P95 error는 23.718 m에서 12.992 m로 감소하였다.

| Metric       | Linear LS K=6 | CF-AQARF | Reduction |
| ------------ | ------------: | -------: | --------: |
| MAE          |       8.311 m |  4.018 m |     51.7% |
| RMSE         |      11.332 m |  6.074 m |     46.4% |
| Median Error |       5.898 m |  2.439 m |     58.6% |
| P95 Error    |      23.718 m | 12.992 m |     45.2% |
| Max Error    |      61.656 m | 37.877 m |     38.6% |

이 결과는 RTT 측정값을 단일 거리 반지름으로 사용하는 LS 방식보다, 앵커별 오차 분위수와 고리형 신뢰 영역을 사용하는 방식이 본 데이터셋에 더 적합함을 보여준다. 특히 Median error가 2.439 m로 감소한 것은 일반적인 샘플에서 CF-AQARF가 안정적인 위치 추정 성능을 보였다는 의미이다.

### 4.4 Baseline 비교의 fairness

Baseline은 최근접 6개 앵커 기반 Linear LS로 설정하였다. 이 방식은 RTT 기반 위치 추정에서 기본적으로 사용할 수 있는 선형화 다변측량 방식이며, 별도의 오차 분위수, 신뢰도장, adaptive band를 사용하지 않는다. 따라서 CF-AQARF가 단순한 거리 방정식 기반 방식에 비해 어떤 개선을 만드는지 확인하는 비교 대상으로 적합하다.

두 방법 모두 동일한 입력인 `d_hat`과 `BS_positions`만 사용한다. CF-AQARF는 추가 센서나 외부 정보를 사용하지 않고, 제공된 700개 labeled data에서 계산한 앵커별 RTT 오차 분위수를 `model.npz`에 저장하여 hidden test에서 사용한다. 5-fold validation에서는 각 fold의 validation 정답 위치를 분위수 계산에 사용하지 않았으므로, validation 성능 비교에서 정답 정보 누수를 피하였다.

### 4.5 파라미터 검토

CF-AQARF는 격자 기반 신뢰도장 탐색을 사용하므로, fine search 해상도, 상위 신뢰 영역 비율, softmax 집중도 등의 파라미터가 결과에 영향을 줄 수 있다. 다만 본 프로젝트에서는 validation 결과를 무작위로 반복 튜닝하여 최고 성능만 선택하는 방식은 피하고, 알고리즘 구조상 의미가 있는 소수의 파라미터만 검토하였다.

| 설정                   | MAE [m] | RMSE [m] | Max [m] | Median [m] | P95 [m] |
| -------------------- | ------: | -------: | ------: | ---------: | ------: |
| 기본값                  |   4.007 |    6.056 |  37.877 |      2.447 |  12.912 |
| fine_step=0.20       |   4.017 |    6.067 |  37.855 |      2.447 |  12.926 |
| fine_top_ratio=0.020 |   3.996 |    6.062 |  37.828 |      2.383 |  12.905 |
| fine_top_ratio=0.015 |   3.985 |    6.062 |  37.843 |      2.375 |  12.873 |
| softmax_beta=10      |   4.007 |    6.057 |  37.874 |      2.456 |  12.912 |
| fine_half_width=10   |   4.009 |    6.035 |  37.828 |      2.503 |  12.556 |

파라미터 검토 결과, 일부 설정에서 MAE나 P95가 소폭 개선되었지만 전체적인 차이는 크지 않았다. 이는 CF-AQARF의 성능이 특정 파라미터 하나에 과도하게 의존하지 않고, 고리형 신뢰도장 구조 자체에서 주로 발생한다는 점을 보여준다. 최종 제출에서는 과도한 validation 튜닝을 피하기 위해 기본 파라미터를 유지하였다.

### 4.6 디스커션

CF-AQARF의 가장 큰 장점은 RTT 오차의 양의 바이어스를 알고리즘 구조에 직접 반영한다는 점이다. 기존 LS는 측정 거리 (\hat{d}_i)를 그대로 실제 거리 반지름으로 사용하지만, CF-AQARF는 학습 데이터에서 계산한 오차 분위수를 이용하여 실제 거리가 존재할 가능성이 높은 고리형 영역을 구성한다. 이로 인해 특정 RTT 측정값이 실제 거리보다 크게 측정되더라도, 알고리즘이 그 값을 무조건 만족해야 하는 거리 제약으로 취급하지 않는다.

또한 CF-AQARF는 최종 위치를 하나의 잔차 제곱합 최소화 문제로 계산하지 않는다. 대신 2차원 공간 전체에 대해 각 후보점의 신뢰도를 계산하고, 여러 앵커가 동시에 지지하는 상위 신뢰 영역의 중심을 최종 위치로 사용한다. 이 방식은 직접 ML 회귀보다 물리적 설명이 가능하고, WLS보다 RTT 오차의 불확실성을 더 명시적으로 표현할 수 있다.

한계도 존재한다. 첫째, CF-AQARF는 격자 기반 탐색을 사용하므로 탐색 범위와 격자 해상도에 영향을 받는다. 본 프로젝트에서는 coarse-to-fine 구조를 사용하여 계산량과 정밀도 사이의 균형을 맞추었지만, hidden test set의 위치 분포가 제공 데이터보다 넓을 경우 탐색 영역 설정이 성능에 영향을 줄 수 있다. 둘째, 신뢰도장이 여러 개의 봉우리로 나뉘는 경우 soft centroid가 중간 위치로 이동할 가능성이 있다. 셋째, 최대 오차는 baseline보다 줄었지만 여전히 37.877 m로 나타났기 때문에 일부 극단 샘플에 대한 추가적인 안정화가 필요하다.

WLS를 최종 보정기로 사용할 수도 있지만, 본 프로젝트에서는 최종 알고리즘에 포함하지 않았다. WLS는 대표적인 위치 추정 방식이지만, 본 데이터셋처럼 RTT 측정값 자체가 양의 바이어스를 가지는 경우에는 신뢰도장으로 얻은 위치를 다시 원래 거리 잔차 방향으로 끌고 갈 수 있다. 또한 WLS는 많은 학생이 사용할 가능성이 높은 일반적인 기법이므로, 본 프로젝트의 핵심 알고리즘으로 삼기에는 참신성이 부족하다고 판단하였다. 따라서 최종 알고리즘은 WLS 후처리 없이 CF-AQARF의 신뢰도장 기반 위치 결정을 그대로 사용하였다.

향후 개선 방향은 세 가지이다. 첫째, 신뢰도장이 여러 봉우리로 형성되는 경우를 감지하여 단일 soft centroid 대신 peak별 후보를 비교하는 구조를 추가할 수 있다. 둘째, 앵커별 분위수뿐 아니라 거리 구간별 분위수를 함께 사용하면 가까운 거리와 먼 거리에서의 오차 차이를 더 정밀하게 반영할 수 있다. 셋째, CF-AQARF 결과의 극단 오차 샘플만을 대상으로 별도의 residual correction을 적용하면 평균 성능을 유지하면서 최대 오차를 줄일 수 있을 것으로 기대된다.

## 5. Reference

본 프로젝트는 수업에서 다룬 LS, WLS, 거리 기반 위치 추정 개념과 제공 데이터에 대한 자체 분석을 기반으로 설계하였다. 별도의 외부 논문 알고리즘을 직접 구현하지 않았으며, 최종 알고리즘인 CF-AQARF는 제공 데이터의 RTT 오차가 양의 바이어스와 긴 꼬리 분포를 갖는다는 분석 결과에서 도출한 자체 설계 방식이다.

참고한 주요 개념은 다음과 같다.

| 개념                     | 본 프로젝트에서의 활용                     |
| ---------------------- | -------------------------------- |
| Least Squares 기반 다변측량  | baseline으로 사용                    |
| Weighted Least Squares | 기존 신뢰도 기반 위치 추정 방식과 비교하는 개념으로 검토 |
| Robust estimation      | 이상치가 위치 추정에 미치는 영향을 해석하는 데 활용    |
| Quantile statistics    | 앵커별 RTT 오차를 평균이 아닌 분위수로 표현       |
| Reliability field      | 후보 위치별 신뢰도 누적을 통한 공간 가능성 지도 구성   |
